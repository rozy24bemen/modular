[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]
cmds = [
  'npm ci --legacy-peer-deps',
  'npm run build',
  'cd server && npm ci --legacy-peer-deps'
]

[start]
cmd = 'node server/index-unified.js'

[variables]
NODE_ENV = 'production'
